import { StyleSheet } from "react-native";
import { Colors } from "@/constants/theme";

export default StyleSheet.create({
  docCard: {
    borderRadius: 10,
    borderWidth: 1,
    borderColor: Colors.Medicle.Grey.Light,
  },
  documentTabWrap: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
  },
  documentTabButton: {

  },
});

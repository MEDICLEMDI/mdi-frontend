import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#FFF',
    },
    listWrap: {
        justifyContent: 'center',
        alignItems: 'center',
    }
});
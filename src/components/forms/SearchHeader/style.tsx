import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  searchBar: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginBottom: 20,
  },
  searchCondition: {
    flexDirection: 'row',
    alignItems: 'center',
  }
});
